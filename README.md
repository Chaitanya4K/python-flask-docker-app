# python-flask-docker-app
A Python Flask web application containerized with Docker. This project demonstrates building a Flask application, creating a Docker image, running the application in a Docker container, and deploying it in a containerized environment.
